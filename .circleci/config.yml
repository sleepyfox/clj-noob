version: 2
jobs:
  build:
    working_directory: ~/clj-noob
    docker:
      - image: clojure
    steps:
      - checkout
      - run: apt-get update && apt-get install -yq make
      - run: lein deps
      - run: lein test
      - run: make build
      - run: make deploy-staging
        
